# 空间文件夹 2.4 开发与验收计划

## 版本定位

2.4 定位为“高强度使用稳定版”。本版本不扩展递归子画布、AI 自动布局和云同步，重点是让现有真实文件功能在大目录、批量操作、版本升级和异常中断场景下保持安全、流畅、可维护。

## 必须满足的产品目标

| 编号 | 目标 | 验收证据 |
|---|---|---|
| R1 | 同一张画布只能由一个 App 进程写入 | 第二个会话获取锁失败并进入只读/阻止写入；自动测试覆盖锁释放和过期进程恢复 |
| R2 | 从 2.3.2 升级后恢复上次文件夹、最近空间和外观 | 偏好迁移测试；首次启动 2.4 后写入稳定偏好域 |
| R3 | 大目录扫描和图标读取不长时间占用主线程 | 扫描服务独立于主模型；性能测试覆盖 3,000 项目录 |
| R4 | 复制、移动、压缩等长操作有进度且不会并发改坏记录 | 串行文件操作协调器；每一步落盘；失败后能恢复或明确进入需核对状态 |
| R5 | 多文件拖入被当作一个批次 | 一次拖入生成一个事务；冲突选择不会被后续回调覆盖 |
| R6 | 多选右键操作作用于整个选择集合 | 复制、剪切、制作副本、压缩、标签和移至废纸篓覆盖选择集合 |
| R7 | 多选拖动保持相对位置 | 使用统一受限位移，不因单个项目碰到边界而压缩或重叠 |
| R8 | 超过 64 项时仍能高效管理待放置项目 | 可搜索、可多选的待放置区面板，不依赖超长菜单 |
| R9 | 测试进入标准工程链路 | `swift test` 可直接执行；GitHub Actions 自动运行测试和严格 Release 构建 |
| R10 | 代码具备人工维护条件 | 新增核心类型和复杂流程包含中文注释；提供完整维护文档和模块关系说明 |

## 工程目标

1. 将 `FolderCanvasModel` 中的会话锁、目录扫描、图标/壁纸加载和真实文件执行拆分为独立类型。
2. 真实文件操作保持“文件系统是事实来源”的原则；画布只保存布局元数据。
3. 所有跨线程结果都在回到主线程前携带会话标识，避免切换文件夹后旧扫描结果覆盖新空间。
4. 2.4 使用稳定 Bundle ID 和稳定 App 名称；以后升级不再通过改 Bundle ID 隔离版本。
5. 发布脚本从一个版本配置读取名称和版本，避免 README、Info.plist 和脚本继续漂移。

## 分阶段实施

### 阶段 A：安全底座

- 画布会话锁。
- 2.3.2 偏好迁移。
- 文件操作逐步骤状态记录与异常恢复入口。

### 阶段 B：性能与架构

- 后台目录扫描和图标缓存。
- 壁纸缓存与按显示尺寸解码。
- 长耗时真实文件操作串行化、进度化。

### 阶段 C：桌面交互完善

- 聚合多文件拖入。
- 多选上下文操作。
- 保持相对位置的多选拖动。
- 待放置区面板。

### 阶段 D：工程化与发布

- 标准测试目标和 CI。
- 2.4 版本打包、签名校验和 GUI 冒烟测试。
- 中文维护文档、发布说明和 README 同步。

## 完成定义

只有当 R1—R10 均有对应代码、测试或运行证据，全部自动测试通过，严格 Release 构建通过，并完成独立 2.4 App 的普通窗口、最大化、全屏和文件操作冒烟验证时，2.4 才视为完成。
