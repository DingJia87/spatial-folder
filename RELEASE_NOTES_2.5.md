# 空间文件夹 v2.5.0

2.5 是面向高强度使用的性能与可恢复性版本。它不改变“一个真实文件夹就是一张空间画布”的产品方向，功能上只增加投放点落位和异常恢复向导，主要投入用于减少写盘、移出主线程 I/O 和拆分主模型。

## 用户可见变化

- 从 Finder 拖入文件时，新图标从鼠标投放点附近按网格落位，不再统一进入默认首个空位。
- 一次拖入多项时，从投放点开始稳定排列；不会挤动画布中的已有项目，没有空位的项目仍进入待放置区。
- 强制退出、崩溃或异常中断后，工具栏会提示“待恢复”。向导展示每个文件步骤的真实磁盘证据，用户可确认已完成、已回滚，或仅存档记录。

## 数据安全

- 文件系统仍是唯一事实来源；恢复向导只读检查磁盘并修正日志解释，不会为“恢复”而移动、覆盖或删除文件。
- 批量操作必须同时具备完整动作覆盖和一致的磁盘证据，才会建议“已完成”。
- 新建项目消失且没有明确废纸篓路径时，不会猜测为“已回滚”。
- 2.4 的操作历史首次读取后会迁移为新格式，原文件保留为 `.2.4.json` 归档。

## 性能与架构

- 操作记录由“每次重写整份 JSON”改为“快照 + JSONL 增量事件”，达到阈值后使用原子快照自动压缩。
- `OperationJournalStore` actor 保证记录串行写入；修改真实文件前会先等待 `pending` 状态落盘。
- 标签、创建、重命名、Office 模板复制和文件属性读取移入后台 actor。
- 布局算法、文件访问、增量日志和恢复分析从主模型拆出，减少后续修改的连锁风险。

## 验证

- 8 项标准 SwiftPM 测试。
- 63 项完整兼容性和真实文件事务回归。
- 严格 Debug/Release 构建，编译警告按错误处理。
- 可重复的 64/500/3,000 项扫描和 1,000 次增量日志基线，详见 `PERFORMANCE_2.5.md`。

## 安装说明

1. 解压 `空间文件夹-v2.5.0.zip`。
2. 将 `空间文件夹.app` 拖入“应用程序”。
3. 首次启动如被 Gatekeeper 阻止，请右键 App 选择“打开”。
4. 首次打开原 2.4 空间时，布局和历史会自动迁移，不会移动用户文件。

当前发布包使用 ad-hoc 本地签名，尚未使用 Developer ID 公证。高强度使用时仍应对重要真实文件保持常规备份。
