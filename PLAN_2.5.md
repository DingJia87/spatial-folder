# 空间文件夹 2.5 开发与验收计划

## 版本定位

2.5 定位为“高强度使用与可维护性版”。以冻结的 2.4 为基线，不扩展递归画布、AI 排列、云同步等新方向，只主攻：增量操作日志、主模型拆分、剩余 I/O 后台化，并加入“拖到哪里就放到哪里”和“异常恢复向导”。

## 不可破坏的产品原则

1. 文件夹与文件始终是真实项目；画布只保存布局元数据。
2. 文件操作必须先记录可恢复的 `pending` 状态，再修改真实文件；每完成一步立即追加日志。
3. 退出、崩溃或断电后，不能把状态未知的操作伪装成成功；必须进入可解释、可处理的恢复流程。
4. 新项目不能挤动已有图标。外部拖入时优先从投放点附近按网格落位，空间不足的项目进入待放置区。
5. 2.4 的画布布局、偏好和操作记录必须无损升级；2.4 分支和发布物保持冻结。

## 必须满足的目标

| 编号 | 目标 | 验收证据 |
|---|---|---|
| R1 | 操作历史改为增量日志 | 每次 upsert/批量更新只追加一条 JSONL 事件，不再全量重写 200 条记录；测试验证事件重放 |
| R2 | 日志可压缩且可从 2.4 迁移 | 旧 `.json` 自动迁移为快照 + 日志；达到阈值后生成原子快照并截断日志；迁移/压缩测试 |
| R3 | 日志持久化不在主线程执行 | `OperationJournalStore` actor 串行写入；真实文件操作启动前 `await` pending 落盘；并发顺序测试 |
| R4 | 主模型职责明显收敛 | 坐标算法、操作日志、真实文件访问/命令从 `FolderCanvasModel` 拆为独立模块；维护文档列明边界 |
| R5 | 剩余真实文件 I/O 后台化 | 标签、创建、模板复制、重命名、属性读取以及生产路径的复制/移动/删除均不在 `@MainActor` 执行 |
| R6 | 外部文件按投放点落位 | 单项落到投放点吸附坐标；多项从该点附近按网格排列；不移动已有项目；溢出进入待放置区 |
| R7 | 异常恢复向导 | 对 `pending/undoing/redoing/unavailable` 记录展示逐项证据、建议动作和明确决策；可标记已完成、已回滚或仅存档 |
| R8 | 保持 2.4 回归安全 | 2.4 的 56 项自测、标准测试与严格 Debug/Release 构建全部通过，并增加 2.5 专项测试 |
| R9 | 性能有可重复基线 | 记录 64/500/3000 项扫描、1000 次日志追加/重放/压缩时间与日志写入字节量 |
| R10 | 形成可交付 2.5 | 版本号、README、变更记录、维护文档、App、ZIP、签名与解压复验一致 |

## 模块目标

```text
FolderCanvasModel（只保留可观察状态和 UI 编排）
├── CanvasLayoutEngine       坐标、吸附、容量、投放点布局
├── OperationJournalStore    actor；增量写入、重放、迁移、压缩
├── FolderAccessRepository   actor；短文件 I/O 与元数据读取
├── FileOperationCoordinator actor；批量/长操作、撤销重做
└── RecoveryAnalyzer         只读分析磁盘证据，生成恢复建议
```

## 实施顺序

1. 建立 2.5 分支、基线测试与计划。
2. 引入操作日志格式、迁移和压缩，先以存储测试证明数据安全。
3. 抽出布局算法和文件访问仓库，再把生产路径的剩余 I/O 接入 actor。
4. 在同一条导入数据流中传递投放坐标，并保存最终布局元数据。
5. 基于日志中的异常记录实现恢复分析与向导，不允许向导直接猜测文件状态。
6. 跑专项测试、全量回归、严格构建、性能基线和 GUI 冒烟测试。
7. 完成审视报告、文档、版本号、打包与签名复验。

## 完成定义

只有 R1—R10 都有当前代码和测试/运行证据，2.4 数据升级安全，真实文件安全顺序未被异步化破坏，并且独立的 2.5 App/ZIP 通过签名和启动验证，才视为 2.5 完成。

## 实施记录（2026-07-16）

| 目标 | 实现与验证证据 |
|---|---|
| R1 | `OperationJournalStore.swift` 实现 upsert/replaceAll JSONL 事件；1,000 次 upsert 回归通过 |
| R2 | 旧 `<key>.json` 迁移为快照并归档为 `.2.4.json`；重放和压缩测试通过 |
| R3 | actor 串行写入，真实文件操作前通过 `persistOperationRecordNow` 等待 pending 落盘 |
| R4 | 已拆出 `CanvasLayoutEngine`、`OperationJournalStore`、`FolderAccessRepository`、`RecoveryAnalyzer`；边界见 `MAINTENANCE_2.5.md` |
| R5 | 生产路径的标签、创建、模板复制、重命名和属性读取均进入 actor；后台短操作回归通过 |
| R6 | `CanvasFileDropDelegate` 传递逻辑投放坐标，布局引擎完成吸附/避让/溢出；已有坐标不变测试通过 |
| R7 | 恢复向导展示逐项证据；批量动作覆盖不全、目标无证据消失和冲突位移均强制人工核对 |
| R8 | 8/8 标准测试、63/63 完整回归通过；严格 Debug/Release 构建通过 |
| R9 | `scripts/run_performance_baseline.zsh` 可重复执行，结果已记入 `PERFORMANCE_2.5.md` |
| R10 | 版本、README、变更记录、发布说明和维护手册已更新；2.5 App/ZIP 已完成签名、解压复验和隔离启动冒烟测试 |
