# 空间文件夹 v2.3.2 测试版

2.3.2 修复全屏模式底部露出灰色窗口背景的问题。

## 修复内容

- 当全屏或最大化视口高于固定逻辑画布时，显示背景自动延伸到可用区域底部。
- 系统桌面壁纸、自定义壁纸和网格背景不会再在底部突然中断。
- 普通窗口仍保持画布比例。
- 最大化窗口仍使用显示器完整可见区域。
- 系统全屏仍解除普通窗口比例约束。

## 空间数据保持不变

- 图标位置、图标大小、字体大小和网格吸附不变。
- 背景延伸只是显示处理，不扩大或改写保存的逻辑画布。
- 文件操作、撤销/重做和真实文件路径没有修改。

## 验证情况

- 46 项自动测试全部通过。
- Release 模式按“警告即错误”完成编译。
- 安装包签名和解压后签名校验通过。
- GUI 验证覆盖普通窗口和系统全屏；全屏画面已连续覆盖到屏幕底部。

2.3.2 使用独立 App 名称和标识，不会覆盖 2.2、2.3 或 2.3.1。不要同时使用多个版本编辑同一个真实文件夹。
