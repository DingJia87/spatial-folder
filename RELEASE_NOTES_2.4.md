# 空间文件夹 v2.4.0

2.4 是面向高强度日常使用的稳定性版本。它不改变“一个真实文件夹就是一张空间画布”的产品方向，重点解决大目录、批量文件操作、多开、升级和长期维护问题。

## 用户可见变化

- 同一张画布同时只能由一个 App 进程编辑。第二个会话仍可浏览和打开文件，但会明确显示只读状态。
- 多文件从 Finder 一次拖入时，只出现一个批次、一条操作记录和一次同名冲突选择。
- 框选多个项目后，右键的复制、剪切、制作副本、压缩、分享、标签、大小、待放置区和废纸篓会作用于整个选择集合。
- 多选拖动保持图标之间的相对位置；整组在画布边缘统一停止。
- 待放置区改为独立面板，可搜索、全选当前结果、多选并批量放回。
- 长耗时复制、移动、压缩、废纸篓和撤销/重做显示进度；允许安全取消的操作会在当前系统调用结束后回滚本批次。
- 目录刷新和壁纸加载不再长时间占用界面线程，大文件夹打开更平稳。

## 数据安全

- 文件系统始终是事实来源；画布只保存位置、大小、待放置状态和壁纸等元数据。
- 每个真实文件步骤完成后立即更新操作记录；崩溃后不会把未完成批次伪装成成功。
- 批量操作中途失败时优先自动回滚；无法完整回滚会进入“需核对”状态，并停止猜测性修改。
- 删除仍然调用 macOS 废纸篓，绝不是只删除画布链接。
- 2.4 读取固定的 Application Support 数据目录，并迁移旧版入口偏好；不会移动用户文件。

## 性能与验证

- 标准测试：6 项，覆盖跨进程锁、3,000 项扫描、批量失败回滚和窗口状态。
- 完整回归：56 项，覆盖布局、真实文件事务、撤销/重做、跨屏、全屏、升级和批量交互。
- 编译要求：Swift 6 严格并发检查，警告按错误处理。
- 系统要求：macOS 14 或更高版本，Apple Silicon Mac。

## 安装说明

1. 解压 `空间文件夹-v2.4.0.zip`。
2. 将 `空间文件夹.app` 拖入“应用程序”。
3. 首次启动如被 Gatekeeper 阻止，请右键 App 选择“打开”。
4. 2.4 会尝试恢复旧测试版最后使用的文件夹、最近空间和外观设置。

当前发布包使用本地临时签名，尚未使用 Developer ID 公证。首次交给朋友使用前，建议保留真实文件的常规备份。
