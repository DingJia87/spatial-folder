# 空间文件夹 v2.3.1 测试版

2.3.1 专门修复窗口比例与最大化显示问题，不改变真实文件操作和已保存的图标坐标。

## 修复内容

- 普通窗口保持基准画布比例：改变宽度时高度联动，改变高度时宽度联动。
- 工具栏使用固定显示高度，不参与画布比例拉伸。
- macOS“缩放/最大化窗口”解除普通窗口比例约束，使用当前显示器完整可见区域。
- 系统全屏继续解除窗口比例约束并填满屏幕。
- 退出最大化或全屏后恢复普通窗口比例。
- 窗口尺寸受当前显示器可见范围保护，不会被调整到屏幕之外。

## 安全边界

- 2.3.1 使用独立 App 名称和标识，不会覆盖 2.2 或原 2.3 测试版。
- 只修改窗口与画布显示层，不修改文件路径、布局坐标和真实文件操作。
- 不要同时使用两个版本编辑同一个真实文件夹。

## 验证情况

- 44 项自动测试全部通过。
- Release 模式按“警告即错误”完成编译。
- 安装包签名和解压后签名校验通过。
- GUI 验证覆盖：普通窗口放大/缩小、最大化、恢复最大化、进入全屏、退出全屏、图标选择、图标拖动和画布右键菜单。
