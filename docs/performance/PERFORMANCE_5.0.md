# 指针空间 5.0 性能说明

5.0 不增加索引、扫描范围或常驻服务。前台标签核对保持 4.2 已验证基线：普通 64 项画布每秒核对一次，单轮约 0.53 毫秒；持续筛选的 3,000 项极端场景每五秒全量兜底，单轮约 28.88 毫秒。

5.0 新增的生命周期规则使窗口隐藏、关闭或最小化时标签 timer 不存在；重新显示后立即补查并恢复前台周期。因此不可见状态的定时标签核对开销由低占用进一步降为零。

2026-08-01 正式候选实测：

| 场景 | 结果 |
| --- | ---: |
| 64 项扫描 | 0.00502 秒 |
| 500 项扫描 | 0.02401 秒 |
| 3,000 项扫描 | 0.14210 秒 |
| 1,000 次增量日志追加 | 0.09745 秒 |
| 日志重放 | 0.000089 秒 |

结果与 4.2 基线处于同一量级，无明显退化。复验命令：

```zsh
./scripts/run_performance_baseline.zsh
```

实际结果在冻结前补充。
