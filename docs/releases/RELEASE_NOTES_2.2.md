# 空间文件夹 v2.2 测试版

2.2 的主题是“真实文件操作可恢复”。它保留 2.1.1 的画布、跨屏稳定性和 Finder 式交互，新增持久化操作记录、统一撤销/重做和显式同名冲突保护。

## 重点变化

- `Command+Z` 撤销最新的布局或文件操作；`Shift+Command+Z` 重做。
- 撤销“移至废纸篓”会恢复真实文件和原画布位置。
- 新建文件夹、Office 文档、副本、压缩包和导入文件均可撤销/重做。
- 粘贴、拖入或重命名出现同名项目时，必须选择保留两者、替换或取消。
- 最近操作面板可查看操作状态并导出隐私安全诊断信息。

## 安装

1. 解压 `空间文件夹-v2.2-测试版.zip`。
2. 将 `空间文件夹 2.2 测试版.app` 拖入“应用程序”。
3. 首次启动时右键 App 选择“打开”。

2.2 使用独立的 App 标识和名称，不会覆盖冻结的 2.1.1 App。两个版本仍会读取同一份画布布局，请不要同时编辑同一空间。

## 测试范围

- 40 项自动回归测试。
- Swift “警告即错误”完整编译。
- 独立 App GUI 验证：文件夹选择、系统壁纸、图标拖动、统一撤销/重做、冲突选择、最近操作、诊断导出和重启恢复。

## 已知边界

- 布局撤销快照仅在当前运行期间有效；文件操作记录可跨重启。
- 文件若被其他 App 再次移动或删除，撤销会停止并报错，不会猜测或覆盖。
- 安装包是本地签名，尚未经 Apple Developer ID 公证。
