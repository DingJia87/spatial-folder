# 变更记录

## v5.3.0 - 真实文件写入安全与拖动稳定版（2026-08-09）

- 所有真实文件写入入口统一使用当前空间授权边界，覆盖重命名、复制/移动、压缩、废纸篓、新建、标签和撤销/重做。
- 整批操作在首次修改磁盘前预检全部路径；撤销/重做同时验证动作记录和废纸篓位置。
- 新增文件修改与日志落盘之间的故障注入回归，覆盖重启证据核对和可捕获失败回滚。
- 修复顶部空间标签拖动闪烁：单枚标签独立刷新，文字、背景与描边整体移动，排序仍只在松手时保存。
- 不新增监听、轮询、索引、遥测或常驻缓存。

## v5.2.2 - 写入安全闭环（候选，2026-08-09）

- 压缩、废纸篓、新建、标签、复制/移动和撤销/重做统一携带当前空间授权上下文。
- 所有真实文件写入在第一次磁盘修改前完成整批路径边界预检，避免新入口绕过 5.2.1 的保护。
- 撤销/重做仅允许当前空间的动作记录，并验证记录中的废纸篓位置，阻止伪造日志作用到无关文件。
- 新增“文件已修改、日志尚未落盘”故障注入回归：重启后根据 pending 记录和磁盘证据进入人工核对；可捕获的落盘失败自动回滚。
- 修复顶部空间标签拖动时整条工具栏反复重算、文字与背景错帧导致的明显闪烁；排序仍只在松手时写入。
- 不新增监听、轮询、索引或常驻缓存；22/22 标准测试、82/82 完整自测通过。

## v5.2.1 - 真实文件边界安全修复（候选，2026-08-09）

- 重命名拒绝空名、“.”、“..”、路径分隔符和 NUL，防止名称被解释为上级路径或子路径。
- 复制、移动、替换和重命名的后台协调器在任何真实文件变更前，一次性校验整批目标都是当前空间的直接子项。
- 制作副本和重命名额外要求来源仍在当前空间第一层，防止异步准备期间切换空间后错作用。
- 打包脚本默认拒绝覆盖已有 App 或 ZIP，候选包可用明确后缀与正式包区分。
- 新增越界重命名、越界替换“零变更”和冻结包防覆盖验证；20/20 标准测试、80/80 完整自测通过。

## v5.2.0 - 多空间快捷切换（2026-08-09）

- 顶部按打开顺序保留最多 5 个空间快捷入口，点击只改变高亮，不再把当前空间移动到首位。
- 打开第 6 个新空间时先选择替换哪一个位置；确认前不切换画布，替换后其他位置保持不变。
- 支持拖动调整空间位置并跨重启保存；窄窗口中暂时放不下的入口进入“更多空间”菜单。
- 修复两个空间拖动排序看似无效的问题；新增独立拖动手柄，源空间会占据目标位置，中间入口顺移。
- 每个空间独立持久化最近 3 步布局撤销与重做，切换空间、移除顶部入口或重启 App 后仍可继续。
- 首次升级会从最近空间中选取最多 5 个并按历史打开次序排列。
- 始终只有一个活动空间：切换时取消旧扫描、监听、标签定时器和图标缓存，再加载目标空间。
- 文件操作进行中禁止切换，延续现有真实文件安全边界。

## v5.1.0 - 可视化布局历史（2026-08-08）

- “布局维护”新增“布局历史”，按时间和生成原因显示最多 12 份布局备份。
- 备份可直接预览空间分布，并与当前布局比较调整、不变、新增和已不存在项目数量。
- 支持恢复任意一份布局备份；确认前明确说明影响，恢复前自动备份当前布局。
- 恢复布局默认保留当前壁纸和锁定状态；可选择同时恢复当时的外观设置。
- 布局备份记录已不存在的文件时只显示提示并忽略坐标，不创建、复制或恢复任何真实文件；备份后新增的文件保持当前位置。
- 支持手动保存带备注的当前布局快照；旧版 JSON 备份继续兼容。
- 修复手动快照错误读取“上一次磁盘布局”并带入失效坐标的问题；新快照严格以当前画布项目生成，保存后立即比较应全部一致。
- 保存布局时自动填写包含日期和时间的推荐名称；布局历史支持确认后删除所选备份，不影响当前布局和真实文件。
- 精简“布局维护”菜单：导入、导出和显示备份位置移入“布局历史 → 更多”，外层只保留布局历史与重置入口。
- 顶部新增独立撤销、重做按钮，可连续前后三步及更多；所有顶部控件提供具体悬停说明，撤销与重做动态显示下一项操作。

## v5.0.0 - 指针空间品牌与轻量化（2026-08-01）

- 产品从“空间文件夹”正式更名为“指针空间”，App、菜单栏、窗口、安装包和用户文案统一更新。
- 保留稳定 Bundle ID、Swift 模块和 `Application Support/SpatialFolder` 数据目录，已有布局、偏好和操作记录无缝沿用。
- 主窗口隐藏、关闭或最小化时暂停 Finder 标签定时核对，重新显示时立即补查。
- GitHub 主页改为面向用户的产品介绍，历史功能细节保留在版本记录与分类文档中。
- 新增用户指南、隐私说明、支持入口、安全政策及 5.0 维护文档。

## v4.3.0 - 收纳前确认（2026-07-26）

- 收纳桌面前显示总数、文件数、文件夹数和目标空间。
- 顶部按钮、菜单栏入口和 `⇧⌘D` 使用同一确认流程。
- 取消或关闭确认卡片不会移动任何真实文件。
- 确认后沿用可撤销的批量移动、事务记录与失败回滚。

## v4.2.0 - 标签同步稳定版（2026-07-26）

- 修复 Finder 等外部位置修改标签后，画布必须重启或重新选择文件夹才更新的问题。
- 文件夹图标本体按 Finder 标签着色；普通文件继续保留系统图标并显示颜色点。
- 修复清除标签后从旧视图再次选择颜色时，旧标签可能被一并写回的问题。
- 顶部筛选和右键标签菜单统一为原色圆点加文字；选中颜色显示同色勾选圆点。
- 标签核对使用可见项每秒检查、筛选状态下全目录每五秒兜底，兼顾即时性和资源占用。
- 标准测试扩展为16项，完整自测71项；严格Release构建、性能基线、签名和ZIP解压复验通过。

## v4.1.0 - 全局快捷唤醒（2026-07-26）

- 新增可自定义的全局显示/隐藏快捷键，默认使用 `⌃⌥空格`。
- App 在后台时快捷键会恢复并置顶主窗口；主窗口已在最前时再次按下会隐藏 App。
- 新增菜单栏常驻入口，提供窗口切换、收纳桌面、最近空间、快捷键设置和退出。
- 快捷键冲突会阻止替换并保留原设置，不需要辅助功能或输入监控权限。
- 主窗口改为单实例，关闭后仍可从快捷键或菜单栏重新打开。
- 测试扩展为 15 项标准测试和 71 项完整回归。

## v4.0.0 - 一键收纳桌面（2026-07-25）

- 顶部新增“收纳桌面”按钮，并支持 `⇧⌘D` 快捷键。
- 一次点击把桌面第一级可见文件和文件夹整体移动到当前空间；文件夹内部结构保持不变。
- 新项目在画布右侧中下部叠放为一摞，并显示数量角标；逐个拖开即可自行摆放，已有图标不移动。
- 收纳点被旧图标占用时，整堆只寻找一次附近空位；超出主画布容量的项目进入待放置区。
- 同名项目自动保留两者；隐藏项目、未完成下载以及包含当前目标空间的桌面祖先项目不会被移动。
- 整批操作进入真实文件事务记录，可一次撤销；锁定画布时仍可收纳，但不会改变已有布局。
- 测试扩展为 12 项标准测试和 70 项完整回归。

## v3.0.0 - 工具栏与外观体验版（2026-07-25）

- 将文件夹选择、当前空间名称和最近空间合并为一个空间菜单。
- 顶部仅保留空间、搜索、标签、条件状态、刷新和“更多”；低频外观与维护命令按用途分组。
- 修复画布锁定时“使用系统桌面壁纸”和自定义壁纸均被静默拦截的问题。
- 壁纸设置拥有独立写入条件；只读、损坏或不可用画布仍保持保护。
- 增加锁定状态下壁纸切换、重启持久化和恢复系统壁纸的回归覆盖；完整自测增至 68 项。

## v2.6.0 - 标签筛选与操作安全版（2026-07-21）

- 顶部移除“新建文件夹”快捷按钮，右键新建和真实文件事务能力保持不变。
- 搜索框旁新增七种 Finder 标签颜色和“无标签”筛选；多颜色使用 OR，与搜索使用 AND。
- 后台扫描保留 Finder 原生颜色编号，自定义标签名称和系统语言不再影响颜色判断。
- 修复筛选隐藏项目后，隐藏选择仍可能进入剪切、压缩、标签或废纸篓等批量命令的问题。
- 顶部筛选只作用于主画布，待放置区继续使用独立搜索。
- 测试扩展为 10 项标准测试和 67 项完整回归；严格 Debug/Release 构建、三轮性能基线和隔离 GUI 冒烟通过。

## v2.5.0 - 高强度使用与可维护性版（2026-07-16）

- 操作记录从每次全量重写改为原子快照 + JSONL 增量日志，并支持自动压缩。
- 2.4 历史记录自动迁移且保留归档副本；日志写入由 actor 串行处理。
- 新增异常恢复向导，对未完成、撤销中和重做中的操作展示逐项磁盘证据；证据不足时不自动修改文件。
- 从 Finder 拖入的文件现在从鼠标投放点附近落位，多项按网格排列，不移动已有项目。
- 拆出 `CanvasLayoutEngine`、`FolderAccessRepository`、`OperationJournalStore` 和 `RecoveryAnalyzer`。
- 标签、创建、重命名、Office 模板复制和文件属性读取的生产路径移到后台执行。
- 增加可重复的 64/500/3,000 项扫描与 1,000 次日志追加性能基线。
- 测试扩展为 8 项标准测试和 63 项完整回归。

## v2.4.0 - 高强度使用稳定版（2026-07-15）

- 同一张画布增加跨进程独占锁；第二个 App 会话进入只读，避免同时写坏布局和操作记录。
- 从 2.3.2 及更早测试版迁移上次文件夹、最近空间和外观偏好，2.4 起使用稳定 Bundle ID。
- 文件夹扫描、壁纸解码和长耗时真实文件操作移出主线程；3,000 项目录有性能回归测试。
- 复制、移动、压缩、废纸篓和撤销/重做由串行协调器执行，显示进度，并在每个文件步骤完成后落盘。
- 多文件拖入合并为一个事务；多选右键支持复制、剪切、副本、压缩、分享、标签、大小和废纸篓。
- 多选拖动使用统一吸附位移，碰到边界时保持组内相对位置。
- 待放置区升级为可搜索、可多选和批量放回的独立面板。
- 新增标准 `swift test` 测试目标、GitHub Actions 严格构建和 56 项完整回归。
- 新增中文维护文档、版本统一配置与独立的 2.4 发布目录。

## v2.3.2 - 全屏背景修复测试版（2026-07-15）

- 修复全屏或最大化视口比逻辑画布更高时，画布底部露出系统灰色背景的问题。
- 可见背景层会扩展到完整视口，壁纸或网格连续覆盖到底部。
- 图标、字体、间距仍按原逻辑画布统一缩放，不进行纵向拉伸。
- 不修改已保存的图标坐标，也不把额外显示区域写入布局数据。
- 自测扩展到 46 项，并完成普通窗口和系统全屏 GUI 视觉验证。

## v2.3.1 - 窗口显示修复测试版（2026-07-15）

- 普通窗口按照基准画布比例联动调整宽高，避免底部出现大块空白。
- macOS“缩放/最大化窗口”使用当前显示器完整可见区域，不再因普通窗口比例约束而在左右留空。
- 系统全屏继续解除窗口比例约束；退出最大化或全屏后恢复普通窗口比例。
- 最大化、全屏和普通窗口切换只改变显示尺寸，不改写文件图标坐标。
- 自测扩展到 44 项，并完成普通窗口、最大化、全屏和恢复窗口 GUI 回归。

## v2.2.0 - 文件安全测试版（2026-07-15）

- 新增持久化文件事务记录，并与布局修改按时序统一撤销/重做。
- 重命名、移至废纸篓、新建、副本、粘贴/导入、压缩和标签纳入可恢复记录。
- 同名项目必须明确选择保留两者、替换或取消；替换操作可完整回滚。
- 新增“最近操作”面板、跨重启文件撤销、未完成操作核对状态与损坏记录存档恢复。
- 根文件夹移动后自动迁移可逆操作路径。
- 新增不包含文件名和完整路径的隐私诊断导出。
- 自测扩展到 40 项，并完成独立 App GUI 冒烟测试。

## v2.1.1 - 轻度使用测试版（2026-07-15）

- 每个文件夹使用固定逻辑画布；显示器切换不再改写或保存图标坐标。
- 小屏幕以单一比例缩放整个画布，图标、字体、间距和壁纸保持相对关系。
- 保持纵向滚动，不引入横向滚动条。
- 支持手动把当前显示器设为基准画布，并提供撤销和迁移前备份。
- 布局格式升级到 v5；旧版小画布在更大显示器打开时可自动迁移。
- 新增跨屏往返、视口缩放、重启稳定性和旧布局迁移测试；共覆盖 18 项可靠性场景。

## v2.1.0 - 本地测试版（2026-07-15）

- 布局自动备份、损坏恢复、导入、导出和最近备份恢复。
- 支持布局撤销、重做、锁定和越界项目找回。
- 画布保持一屏大小，超过 8×8 容量的项目进入待放置区。
- 文件被 Finder 重命名后通过资源标识迁移原位置。
- 根文件夹移动后通过书签自动恢复，并支持手动重新关联。
- 新增独立布局可靠性自测程序。

## v2.0.0 - 2026-07-14

- 最近空间入口与当前画布筛选，不改动图标布局。
- 新加入的文件进入可预测的空闲位置，不打乱已有图标。
- 读取和设置 Finder 原生颜色标签，并在图标下显示标签点。
- 右键补充压缩、分享和文件简介。
- 文件夹监听增加防抖；文件图标加入缓存。

## v1.1.0 - 2026-07-14

- 自动恢复上次打开的文件夹。
- 支持跟随系统、浅色和深色外观。
- 支持从 Finder 拖入、画布内复制剪切粘贴和制作副本。
- 修复双击文件夹后在 Finder 中打开。

## v1.0.0 - 2026-07-14

- 实现空间化第一级文件夹画布。
- 保存图标位置和大小。
- 支持框选、多选拖动、网格吸附和壁纸。
- 支持真实文件打开、重命名、移至废纸篓。
- 支持静默创建 Excel、Word、PowerPoint 空白文件。
